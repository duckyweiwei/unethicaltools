# unethicaltools

Clean-slate Next.js (App Router) app. Deploys to https://www.unethicaltools.com/
via Vercel — auto-deploys on every push to `main`.

## Develop

```bash
npm install
npm run dev
```

Then open http://localhost:3000.

## Build

```bash
npm run build
```
